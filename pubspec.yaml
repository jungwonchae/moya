name: moya_app
description: MOYA 생리 관리 앱

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.1.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  get: ^4.6.6
  flutter_bluetooth_serial: ^0.4.0
  permission_handler: ^11.0.1
  shared_preferences: ^2.2.0
  camera: ^0.10.5+5
  image_picker: ^1.0.4
  path_provider: ^2.1.1
  sqflite: ^2.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true